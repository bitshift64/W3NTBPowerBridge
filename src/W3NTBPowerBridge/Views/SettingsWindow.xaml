<Window x:Class="W3NTBPowerBridge.Views.SettingsWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="Settings"
        Height="680"
        Width="640"
        MinHeight="560"
        MinWidth="600"
        WindowStartupLocation="CenterOwner"
        ResizeMode="CanResize"
        Background="{DynamicResource WindowBackgroundBrush}"
        Foreground="{DynamicResource PrimaryTextBrush}">
  <Window.Resources>
    <Style TargetType="TextBlock">
      <Setter Property="Foreground" Value="{DynamicResource PrimaryTextBrush}" />
    </Style>
    <Style TargetType="CheckBox">
      <Setter Property="Foreground" Value="{DynamicResource PrimaryTextBrush}" />
      <Setter Property="Margin" Value="0,0,0,10" />
    </Style>
    <Style TargetType="TextBox">
      <Setter Property="Foreground" Value="{DynamicResource PrimaryTextBrush}" />
      <Setter Property="Background" Value="{DynamicResource InputBackgroundBrush}" />
      <Setter Property="BorderBrush" Value="{DynamicResource InputBorderBrush}" />
      <Setter Property="MinHeight" Value="24" />
    </Style>
    <Style TargetType="Button">
      <Setter Property="Foreground" Value="{DynamicResource PrimaryTextBrush}" />
      <Setter Property="Background" Value="{DynamicResource PanelBackgroundBrush}" />
      <Setter Property="BorderBrush" Value="{DynamicResource PanelBorderBrush}" />
    </Style>
    <Style TargetType="GroupBox">
      <Setter Property="Foreground" Value="{DynamicResource PrimaryTextBrush}" />
      <Setter Property="BorderBrush" Value="{DynamicResource PanelBorderBrush}" />
      <Setter Property="Margin" Value="0,0,0,14" />
      <Setter Property="Padding" Value="12" />
    </Style>
    <Style TargetType="TabControl">
      <Setter Property="Background" Value="{DynamicResource WindowBackgroundBrush}" />
      <Setter Property="BorderBrush" Value="{DynamicResource PanelBorderBrush}" />
      <Setter Property="Template">
        <Setter.Value>
          <ControlTemplate TargetType="TabControl">
            <DockPanel LastChildFill="True">
              <TabPanel DockPanel.Dock="Top"
                        IsItemsHost="True"
                        KeyboardNavigation.TabIndex="1"
                        Background="{DynamicResource WindowBackgroundBrush}" />
              <Border Background="{DynamicResource PanelBackgroundBrush}"
                      BorderBrush="{DynamicResource PanelBorderBrush}"
                      BorderThickness="1"
                      Padding="12">
                <ContentPresenter ContentSource="SelectedContent" />
              </Border>
            </DockPanel>
          </ControlTemplate>
        </Setter.Value>
      </Setter>
    </Style>
    <Style TargetType="TabItem">
      <Setter Property="Foreground" Value="{DynamicResource PrimaryTextBrush}" />
      <Setter Property="Padding" Value="14,6" />
      <Setter Property="Margin" Value="0,0,4,0" />
      <Setter Property="Template">
        <Setter.Value>
          <ControlTemplate TargetType="TabItem">
            <Border x:Name="TabBorder"
                    Background="{DynamicResource PanelBackgroundBrush}"
                    BorderBrush="{DynamicResource PanelBorderBrush}"
                    BorderThickness="1"
                    Padding="{TemplateBinding Padding}">
              <ContentPresenter x:Name="ContentSite"
                                ContentSource="Header"
                                HorizontalAlignment="Center"
                                VerticalAlignment="Center"
                                RecognizesAccessKey="True" />
            </Border>
            <ControlTemplate.Triggers>
              <Trigger Property="IsSelected" Value="True">
                <Setter TargetName="TabBorder" Property="Background" Value="{DynamicResource WindowBackgroundBrush}" />
                <Setter TargetName="TabBorder" Property="BorderBrush" Value="{DynamicResource InputBorderBrush}" />
              </Trigger>
              <Trigger Property="IsMouseOver" Value="True">
                <Setter TargetName="TabBorder" Property="BorderBrush" Value="{DynamicResource InputBorderBrush}" />
              </Trigger>
            </ControlTemplate.Triggers>
          </ControlTemplate>
        </Setter.Value>
      </Setter>
    </Style>
    <Style x:Key="FieldLabel" TargetType="TextBlock">
      <Setter Property="Margin" Value="0,0,12,10" />
      <Setter Property="VerticalAlignment" Value="Center" />
    </Style>
    <Style x:Key="FieldTextBox" TargetType="TextBox" BasedOn="{StaticResource {x:Type TextBox}}">
      <Setter Property="Margin" Value="0,0,0,10" />
    </Style>
  </Window.Resources>

  <Grid Margin="18">
    <Grid.RowDefinitions>
      <RowDefinition Height="*" />
      <RowDefinition Height="Auto" />
    </Grid.RowDefinitions>

    <TabControl Grid.Row="0">
      <TabItem Header="Connections">
        <ScrollViewer VerticalScrollBarVisibility="Auto">
          <StackPanel Margin="14">
            <GroupBox Header="ACLog">
              <Grid>
                <Grid.ColumnDefinitions>
                  <ColumnDefinition Width="150" />
                  <ColumnDefinition Width="*" />
                </Grid.ColumnDefinitions>
                <Grid.RowDefinitions>
                  <RowDefinition Height="Auto" />
                  <RowDefinition Height="Auto" />
                  <RowDefinition Height="Auto" />
                </Grid.RowDefinitions>

                <TextBlock Grid.Row="0" Grid.Column="0" Style="{StaticResource FieldLabel}" Text="Host" />
                <TextBox Grid.Row="0" Grid.Column="1" Style="{StaticResource FieldTextBox}" Text="{Binding AcLogHost}" />
                <TextBlock Grid.Row="1" Grid.Column="0" Style="{StaticResource FieldLabel}" Text="Port" />
                <TextBox Grid.Row="1" Grid.Column="1" Style="{StaticResource FieldTextBox}" Text="{Binding AcLogPort}" />
                <TextBlock Grid.Row="2" Grid.Column="0" Style="{StaticResource FieldLabel}" Text="ACLog.exe path" />
                <TextBox Grid.Row="2" Grid.Column="1" Style="{StaticResource FieldTextBox}" Text="{Binding AcLogPath}" />
              </Grid>
            </GroupBox>

            <GroupBox Header="wfview client">
              <Grid>
                <Grid.ColumnDefinitions>
                  <ColumnDefinition Width="150" />
                  <ColumnDefinition Width="*" />
                </Grid.ColumnDefinitions>
                <Grid.RowDefinitions>
                  <RowDefinition Height="Auto" />
                  <RowDefinition Height="Auto" />
                  <RowDefinition Height="Auto" />
                </Grid.RowDefinitions>

                <TextBlock Grid.Row="0" Grid.Column="0" Style="{StaticResource FieldLabel}" Text="Host" />
                <TextBox Grid.Row="0" Grid.Column="1" Style="{StaticResource FieldTextBox}" Text="{Binding WfviewHost}" />
                <TextBlock Grid.Row="1" Grid.Column="0" Style="{StaticResource FieldLabel}" Text="Port" />
                <TextBox Grid.Row="1" Grid.Column="1" Style="{StaticResource FieldTextBox}" Text="{Binding WfviewPort}" />
                <TextBlock Grid.Row="2" Grid.Column="0" Style="{StaticResource FieldLabel}" Text="wfview.exe path" />
                <TextBox Grid.Row="2" Grid.Column="1" Style="{StaticResource FieldTextBox}" Text="{Binding WfviewPath}" />
              </Grid>
            </GroupBox>

            <GroupBox Header="Shack wfview server">
              <Grid>
                <Grid.ColumnDefinitions>
                  <ColumnDefinition Width="150" />
                  <ColumnDefinition Width="*" />
                </Grid.ColumnDefinitions>
                <Grid.RowDefinitions>
                  <RowDefinition Height="Auto" />
                  <RowDefinition Height="Auto" />
                </Grid.RowDefinitions>

                <TextBlock Grid.Row="0" Grid.Column="0" Style="{StaticResource FieldLabel}" Text="Server IP / host" />
                <TextBox Grid.Row="0" Grid.Column="1" Style="{StaticResource FieldTextBox}" Text="{Binding WfviewServerHost}" />
                <TextBlock Grid.Row="1" Grid.Column="0" Style="{StaticResource FieldLabel}" Text="Server wfview path" />
                <TextBox Grid.Row="1" Grid.Column="1" Style="{StaticResource FieldTextBox}" Text="{Binding WfviewServerPath}" />
              </Grid>
            </GroupBox>
          </StackPanel>
        </ScrollViewer>
      </TabItem>

      <TabItem Header="Startup">
        <ScrollViewer VerticalScrollBarVisibility="Auto">
          <StackPanel Margin="14">
            <GroupBox Header="Startup actions">
              <StackPanel>
                <CheckBox Content="Start full station sequence on startup" IsChecked="{Binding AutoStartStationOnStartup}" />
                <CheckBox Content="Connect automatically on startup" IsChecked="{Binding AutoConnectOnStartup}" />
                <CheckBox Content="Launch wfview automatically" IsChecked="{Binding AutoLaunchWfview}" />
                <CheckBox Content="Launch ACLog automatically" IsChecked="{Binding AutoLaunchAcLog}" />
                <CheckBox Content="Launch shack server during Start Station" IsChecked="{Binding LaunchWfviewServerDuringStartStation}" />
              </StackPanel>
            </GroupBox>

            <GroupBox Header="Startup timing">
              <Grid>
                <Grid.ColumnDefinitions>
                  <ColumnDefinition Width="220" />
                  <ColumnDefinition Width="120" />
                  <ColumnDefinition Width="*" />
                </Grid.ColumnDefinitions>
                <Grid.RowDefinitions>
                  <RowDefinition Height="Auto" />
                  <RowDefinition Height="Auto" />
                  <RowDefinition Height="Auto" />
                  <RowDefinition Height="Auto" />
                </Grid.RowDefinitions>

                <TextBlock Grid.Row="0" Grid.Column="0" Style="{StaticResource FieldLabel}" Text="After power on" />
                <TextBox Grid.Row="0" Grid.Column="1" Style="{StaticResource FieldTextBox}" Text="{Binding StationPowerOnDelaySeconds}" />
                <TextBlock Grid.Row="0" Grid.Column="2" Style="{StaticResource FieldLabel}" Text="seconds" />
                <TextBlock Grid.Row="1" Grid.Column="0" Style="{StaticResource FieldLabel}" Text="Wait for server audio" />
                <TextBox Grid.Row="1" Grid.Column="1" Style="{StaticResource FieldTextBox}" Text="{Binding WfviewServerAudioWaitSeconds}" />
                <TextBlock Grid.Row="1" Grid.Column="2" Style="{StaticResource FieldLabel}" Text="seconds" />
                <TextBlock Grid.Row="2" Grid.Column="0" Style="{StaticResource FieldLabel}" Text="After server launch" />
                <TextBox Grid.Row="2" Grid.Column="1" Style="{StaticResource FieldTextBox}" Text="{Binding WfviewServerSettleDelaySeconds}" />
                <TextBlock Grid.Row="2" Grid.Column="2" Style="{StaticResource FieldLabel}" Text="seconds" />
                <TextBlock Grid.Row="3" Grid.Column="0" Style="{StaticResource FieldLabel}" Text="Between wfview and ACLog" />
                <TextBox Grid.Row="3" Grid.Column="1" Style="{StaticResource FieldTextBox}" Text="{Binding AcLogLaunchDelaySeconds}" />
                <TextBlock Grid.Row="3" Grid.Column="2" Style="{StaticResource FieldLabel}" Text="seconds" />
              </Grid>
            </GroupBox>
          </StackPanel>
        </ScrollViewer>
      </TabItem>

      <TabItem Header="Power">
        <ScrollViewer VerticalScrollBarVisibility="Auto">
          <StackPanel Margin="14">
            <GroupBox Header="Shelly station power">
              <Grid>
                <Grid.ColumnDefinitions>
                  <ColumnDefinition Width="170" />
                  <ColumnDefinition Width="*" />
                </Grid.ColumnDefinitions>
                <Grid.RowDefinitions>
                  <RowDefinition Height="Auto" />
                  <RowDefinition Height="Auto" />
                  <RowDefinition Height="Auto" />
                  <RowDefinition Height="Auto" />
                </Grid.RowDefinitions>

                <CheckBox Grid.Row="0" Grid.Column="1" Content="Enable Shelly station power" IsChecked="{Binding ShellyEnabled}" />
                <TextBlock Grid.Row="1" Grid.Column="0" Style="{StaticResource FieldLabel}" Text="Shelly host" />
                <TextBox Grid.Row="1" Grid.Column="1" Style="{StaticResource FieldTextBox}" Text="{Binding ShellyHost}" />
                <TextBlock Grid.Row="2" Grid.Column="0" Style="{StaticResource FieldLabel}" Text="Port / switch ID" />
                <StackPanel Grid.Row="2" Grid.Column="1" Orientation="Horizontal" Margin="0,0,0,10">
                  <TextBox Text="{Binding ShellyPort}" Width="90" Margin="0,0,10,0" />
                  <TextBox Text="{Binding ShellySwitchId}" Width="90" />
                </StackPanel>
                <TextBlock Grid.Row="3" Grid.Column="0" Style="{StaticResource FieldLabel}" Text="Off threshold watts" />
                <TextBox Grid.Row="3" Grid.Column="1" Style="{StaticResource FieldTextBox}" Text="{Binding StationOffPowerThresholdWatts}" />
              </Grid>
            </GroupBox>

            <GroupBox Header="Radio status sync">
              <Grid>
                <Grid.ColumnDefinitions>
                  <ColumnDefinition Width="170" />
                  <ColumnDefinition Width="*" />
                </Grid.ColumnDefinitions>
                <Grid.RowDefinitions>
                  <RowDefinition Height="Auto" />
                  <RowDefinition Height="Auto" />
                </Grid.RowDefinitions>

                <CheckBox Grid.Row="0" Grid.Column="1" Content="Sync RF power to ACLog power field" IsChecked="{Binding SyncRfPowerToAcLog}" />
                <TextBlock Grid.Row="1" Grid.Column="0" Style="{StaticResource FieldLabel}" Text="Radio max watts" />
                <TextBox Grid.Row="1" Grid.Column="1" Style="{StaticResource FieldTextBox}" Text="{Binding RadioMaxPowerWatts}" />
              </Grid>
            </GroupBox>
          </StackPanel>
        </ScrollViewer>
      </TabItem>

      <TabItem Header="Display">
        <ScrollViewer VerticalScrollBarVisibility="Auto">
          <StackPanel Margin="14">
            <GroupBox Header="Main screen">
              <StackPanel>
                <CheckBox Content="Show event log" IsChecked="{Binding ShowEventLog}" />
                <CheckBox Content="Show ACLog status" IsChecked="{Binding ShowAcLogStatus}" />
                <CheckBox Content="Show wfview status" IsChecked="{Binding ShowWfviewStatus}" />
                <CheckBox Content="Show radio frequency" IsChecked="{Binding ShowRadioFrequency}" />
                <CheckBox Content="Show mode and RF power" IsChecked="{Binding ShowModeAndRfPower}" />
                <CheckBox Content="Show last requested frequency" IsChecked="{Binding ShowLastRequestedFrequency}" />
                <CheckBox Content="Show last confirmed frequency" IsChecked="{Binding ShowLastConfirmedFrequency}" />
                <CheckBox Content="Show tune result" IsChecked="{Binding ShowTuneResult}" />
                <CheckBox Content="Expand station power panel by default" IsChecked="{Binding ExpandPowerPanel}" />
              </StackPanel>
            </GroupBox>

            <GroupBox Header="Theme">
              <CheckBox Content="Use dark mode with red text" IsChecked="{Binding DarkModeEnabled}" />
            </GroupBox>
          </StackPanel>
        </ScrollViewer>
      </TabItem>
    </TabControl>

    <StackPanel Grid.Row="1" Orientation="Horizontal" HorizontalAlignment="Right" Margin="0,14,0,0">
      <Button Content="Cancel" Width="90" Margin="0,0,8,0" Click="Cancel_Click" />
      <Button Content="Save" Width="90" Click="Save_Click" />
    </StackPanel>
  </Grid>
</Window>
